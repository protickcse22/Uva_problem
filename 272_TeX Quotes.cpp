#include<bits/stdc++.h>
using namespace std;
int main()
{
    char s[1000];
    int a,b,cnt=0,x=0;
    while(gets(s))
{
    for(int i=0;i<strlen(s);i++)
    {
        if(s[i]!='"')
            cnt++;
        if(cnt%2!=0)
            s[]
    }
    puts(s);


}
   return 0;
}
